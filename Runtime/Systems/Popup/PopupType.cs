using System;

/// <summary>
/// Enum representing different types of popups in the game.
/// This provides type safety and prevents typos when referencing popup types.
/// </summary>
public enum PopupType
{
    WinLevel = 0,
    LoseLevel = 1,
    BuyCurrency = 2,
    Settings = 3,
    WatchAdForLives = 4,
    SimplePopup = 5,
    RateUsPopup = 6,
    BonusTime = 7,
    Tutorial = 8,  
    Shop = 9,
    LootReward = 10,
    FillLives = 11,
    LoseLevelWarning = 12,
    StartLevel = 13,
    GoHomeConfirm = 14,
    PreBooster = 15,
    SeasonPass = 16
}

